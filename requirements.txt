# Core
onnx>=1.17
onnxruntime-directml>=1.18
numpy>=1.26
pillow>=10

# Optional, install as needed for additional EPs:
# onnxruntime>=1.18            # CPU baseline (already pulled by directml pkg)
# onnxruntime-openvino>=1.18   # Intel NPU / iGPU / dGPU
# onnxruntime-qnn>=1.18        # Qualcomm Hexagon NPU
# onnxruntime-vitisai>=1.18    # AMD Ryzen AI native EP

# Demo 1 (image classification)
#   uses onnx already — no extra deps

# Demo 2 (whisper-tiny int8 transcription)
# huggingface-hub>=0.24        # for downloading the whisper-tiny int8 ONNX

# Developer / test
pytest>=8.0
